# Test environment variables for Playwright E2E tests
# These are placeholder values that allow the app to start without a real Supabase instance

# Leave these empty to skip Supabase auth checks during E2E tests
# The middleware will detect missing values and skip authentication
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Mapbox token (public, for map rendering)
NEXT_PUBLIC_MAPBOX_TOKEN=pk.test_placeholder_token
